# 实施计划落地状态

> 对照日期:2026-08-29 · 分支 `springboot3`。
> 各实施计划文档(`docs/实施计划-*.md`)已于 2026-08-30 删除归档;`docs/superpowers/plans/` / `docs/superpowers/specs/` 原文保留。本表为落地状态的历史记录,**不要按文内「待实施」再开一轮**。
> 动手前先看本表;未做项才值得排期。

| 计划 | 状态 | 代码入口 | 仍未做 / 残留 |
|------|------|----------|----------------|
| 统一智能体工具循环 | **已落地** | `AgentToolLoop`、`ChatTurnRunner`、`SubAgentToolCallback`;工具名是 `bash` 不是计划里的 `runShell` | 无代码缺口 |
| 视觉能力门控 | **已落地** | `AgentContext.visionEnabled`、`ConversationVisionGate`、`AgentToolLoop.appendPromptMedia` | 存量库 `vision_enabled IS NULL` 是否已跑 `sql/fix_ai_model_vision_enabled_null.sql` 需环境确认 |
| 特殊事件通道 | **已落地** | `UiArtifactEmitter`、`ai_chat_special_event`、`SpecialEventService` | `Scope.GLOBAL` 仍拒绝(计划明确不做) |
| 引用归并单行存储 | **已落地** | `KbReferencesUiPayload` v2、`MergePolicy.MERGE`、乐观锁 `version` | 存量 v1 是否已跑 `sql/clean_kb_references_v1.sql` 需环境确认 |
| 会话存储与加载瘦身 | **已落地** | timeline `columnsLite`、`output_data_path` | 无代码缺口 |
| 计量与数据一致性修复 | **部分落地** | `CacheTokens.effectiveHit`、Run 写入改 Java `Date`、回滚清溢出文件、`run_step.message_id` | 首页 `selectCacheStats` 仍 `sum(cache_hit)` 未 clamp;心跳列仍走 `current_timestamp` |
| 提升智能体产出质量 | **部分落地** | Docker 预装 Node/Chromium、`ScreenshotToolCallback`、`composeAssignedUser` | 子智能体 prompt 未改「先确认 / 必须截图」;`askUser` 明确不排期 |
| [kb-ui-artifact](superpowers/plans/2026-08-14-kb-ui-artifact.md) | **已落地后演进** | `ChatEventJson.ui` + `UiArtifactAware` | 投影不再写 `run_step`(改独立表),属后续通道计划,不是回退 |
| [mobile-book-chat](superpowers/plans/2026-08-16-mobile-book-chat.md) | **已删除** | 原 `mobile/` 书本式聊天前端已从仓库移除 | 历史计划/设计稿保留在 `docs/superpowers/`;不要按该计划再开工 |
| [ai-memory 长期记忆](superpowers/specs/2026-08-29-ai-memory-design.md) | **已落地(批次1-4)** | `ai/memory/longterm/` 包、`ChatTurnRunner` 读侧注入、`ContextCompactor` 压缩搭车提炼(走 `MemoryExtractor.persistFacts` 统一落库)、`sql/init/10-12` | 设计文档 §10「条数上限按 last_hit 失活」未实现(`max-per-*-scope` 已配置但无消费方);「agent 层遮蔽用户层」未在检索组装层落地;写侧 embedding 失败无补偿任务;失败退避与搭车位点同步推进已提交 |

全新环境建库走 `sql/init/`(已含 `ai_chat_special_event`、`run_step.message_id`、`output_data_path`、`ai_memory`/`mem_vector_*`/`ai_memory_extract_progress`)。已有库仍用 `sql/` 根目录增量脚本(记忆提炼位点表存量升级加失败退避列走对应 ALTER)。
